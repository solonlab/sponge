kind: Deployment
apiVersion: apps/v1
metadata:
  name: trackapi
  namespace: water
  labels:
    app: trackapi
  annotations:
    deployment.kubernetes.io/revision: '40'
    field.cattle.io/publicEndpoints: >-
      [{"addresses":["10.0.0.193"],"port":31324,"protocol":"TCP","serviceName":"water:trackapi","allNodes":true}]
    kubesphere.io/creator: admin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trackapi
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: trackapi
      annotations:
        kubesphere.io/restartedAt: '2021-12-03T10:05:02.441Z'
    spec:
      volumes:
        - name: host-time
          hostPath:
            path: /etc/localtime
            type: ''
      containers:
        - name: container-ybishc
          image: 'registry.cn-hangzhou.aliyuncs.com/noearorg/trackapi:latest'
          ports:
            - name: http-8080
              containerPort: 8080
              protocol: TCP
          env:
            - name: TZ
              value: Asia/Shanghai
          resources: {}
          volumeMounts:
            - name: host-time
              readOnly: true
              mountPath: /etc/localtime
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
          imagePullPolicy: Always
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst
      serviceAccountName: default
      serviceAccount: default
      securityContext: {}
      affinity: {}
      schedulerName: default-scheduler
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 25%
      maxSurge: 25%
  revisionHistoryLimit: 10
  progressDeadlineSeconds: 600
